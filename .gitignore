# === HASSAS — repo'ya GİRMEZ ===
# CoinGecko/Apify anahtarları
kripto-config.json
# gerçek portföy/pozisyonlar (lokalde kalır)
kripto_portfoy.json
kripto_gecmis.json

# Python
__pycache__/
*.pyc
*.pyo

# Radar runtime (lokal)
radar_state.json
radar_active.json
radar_alerts.log
radar_run.log
radar_archive.jsonl

# Piyasa yapisi runtime (lokal trend logu)
piyasa_yapisi_log.jsonl

# Nobetci runtime (lokal alarm state/log)
nobetci_state.json
nobetci_alarm.log

# TestBot runtime (sanal test verisi, lokal)
testbot_state.json
testbot_islemler.jsonl
testbot_equity.jsonl
veto_log.jsonl
testbot_aday_arsiv.jsonl
btc_pay_log.jsonl
panel_islem_log.jsonl

# "Ben" hesabi — bagimsiz ikinci sanal kasa (2026-08-05)
benim_state.json
benim_islemler.jsonl
benim_equity.jsonl

# Likidasyon akisi runtime (lokal log)
likidasyon_log.jsonl
coinalyze_liq_log.jsonl

# Panel sunucu runtime (lokal log)
panel_out.log
panel_sunucu.log
panel_sunucu.log.err

# TestBot mutex kilidi (runtime, race-condition korumasi)
testbot.lock

# Arsiv (olu kod + gecmis tur-loglari; 2026-07-23 sadelestirme, geri-alinabilir)
arsiv/

# OS / editor
.DS_Store
Thumbs.db

# Olcum scriptlerinin mum onbellegi (buyuk, yeniden uretilebilir)
scratchpad/klines_cache/
scratchpad/*.json

# Golge defter runtime (reddedilen girislerin sanal karnesi, 2026-08-10)
golge_state.json
golge_islemler.jsonl
golge_equity.jsonl

# Config yedekleri — ANAHTAR ICERIR, asla commit edilmez
kripto-config.json.yedek-*

# Olcum scriptlerinin ara ciktilari (buyuk, yeniden uretilebilir)
scratchpad/oruntu_olaylar.json
scratchpad/klines_1h/
scratchpad/klines_1h_uzun/
scratchpad/taker/klines/
scratchpad/fund_ls/klines/
scratchpad/komp_temiz/metrics/
scratchpad/funding_gecmis/
scratchpad/scalp_1m/
*_state.json.yedek-*
*.jsonl.yedek-*
pozisyon_izleme.jsonl
pozisyon_ozet.jsonl
izleyici_state.json
izleyici.lock
scratchpad/kacan_olaylar.json
scratchpad/hedef10_olaylar.json
scratchpad/gainer_olaylar.json
scratchpad/gainer_listesi.json

# Perp 5dk serileri — 30 gunluk pencereden cekilir, BUYUK, yeniden uretilebilir
scratchpad/perp_seri/

# Major 5dk mumlar (BTC/ETH 2 yil)
scratchpad/major_5dk/

# CoinGecko dominans/mcap onbellegi
scratchpad/gecko/

# short_kayip olcum ailesinin kendi onbellekleri
scratchpad/short_kayip/fonlama/
scratchpad/short_kayip/mumlar/

# poz_yol ara veri kumeleri
scratchpad/poz_yol/*.json

# 5dk scalp onbellegi
scratchpad/scalp_5m/

# Perp seri indirici gunluk logu
scratchpad/perp_seri_indir.log

# Ayna defteri runtime — diger dort defterle tutarli olsun (2026-08-24)
ayna_state.json
ayna_islemler.jsonl
ayna_equity.jsonl

# Defter2 runtime
defter2_state.json
defter2_islemler.jsonl
defter2_equity.jsonl

# Olay seviyesi olcum ailesinin ara veri kumesi
scratchpad/olay_seviyesi/*.json

# TimesFM sanal ortami (ayri venv — botun global Python'una DOKUNMAZ)
# TabFM sanal ortami + agirliklar (6,3 GB, yeniden indirilebilir)
scratchpad/tabfm_venv/
scratchpad/tabfm/*.json
scratchpad/tabfm/hf_cache/
scratchpad/tabfm/agirlik/
scratchpad/tabfm/taze_mum/

# Basis yoklamasi ara ciktisi
scratchpad/basis/*.json
scratchpad/basis/spot_1h/
scratchpad/basis/*.log
scratchpad/tabfm/*.log

scratchpad/tfm_venv/

# TimesFM kapsama testi ham ciktisi (buyuk, yeniden uretilebilir)
scratchpad/tfm/ham_sonuc.json

# Stop carpani olcumunun ara veri kumesi
scratchpad/stop_carpani/*.json

# Genislik olcumunun ara veri kumesi
scratchpad/genislik/*.json

# Fonlama onarimi oncesi yedek (2026-08-25, birim kirilmasi)
scratchpad/funding_yedek_20260825/

# Defter2 veto kutugu - defter3'te vardi, defter2'de UNUTULMUSTU (2026-08-25)
defter2_veto.jsonl

# fonlama_rejim ara olay kumesi (2 MB, yeniden uretilebilir)
# NOT: ayni klasordeki seri.json TAKIPTE kalir - zaten commit edilmisti
scratchpad/fonlama_rejim/olaylar.json

# Defter3 runtime (defter2 evreni + iki yon, 2026-08-25)
defter3_state.json
defter3_islemler.jsonl
defter3_equity.jsonl
defter3_veto.jsonl
scratchpad/olay_pencere/
scratchpad/makro_pencere/
scratchpad/aday_pencere_1h/
scratchpad/taze_1h/
scratchpad/taze_funding/

# notrlong defteri (2026-09-06) — state/defter/log ASLA commit edilmez
notrlong_state.json
notrlong_islemler.jsonl
notrlong_equity.jsonl
notrlong_veto.jsonl
notrlong_elenen.jsonl
notrlong_log.txt
notrlong_*_pencere*.jsonl
yedek_pencere1_*/

# Capraz borsa olcumu ham verisi (2026-09-06) — 216 MB, yeniden indirilebilir
# Betikler (00..03_*.py) ve sonuc.txt TAKIPTE kalir; yalniz ham veri dislanir
scratchpad/capraz/veri/
scratchpad/capraz/panel.jsonl

# OBI olcumu ham verisi (2026-09-06) — bookDepth + fiyat serileri
scratchpad/obi/fiyat/
scratchpad/obi/obi/
scratchpad/obi/panel.jsonl

# Uc defter birlestirme onbellegi (uc_defter_birlestir.py uretir, yeniden uretilebilir)
scratchpad/uc_defter.pkl
